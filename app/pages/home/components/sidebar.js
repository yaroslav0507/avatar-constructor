'use strict';

var React = require('react');

var Sidebar = React.createClass({

    render(){
        return (
            <div className="sidebar">
                This is sidebar
            </div>
        );
    }

});

module.exports = Sidebar;
